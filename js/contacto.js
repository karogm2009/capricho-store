import { loadHeader, $, getSettings, escapeHtml } from './common.js'; await loadHeader(); const el=$('#contact-content'); const s=await getSettings().catch(()=>null); if(!s){el.textContent='La información de contacto aún no ha sido configurada.';return;} const links=[];if(s.instagram)links.push(`<p>Instagram: ${escapeHtml(s.instagram)}</p>`);if(s.city)links.push(`<p>Ciudad: ${escapeHtml(s.city)}</p>`);if(s.address)links.push(`<p>Dirección: ${escapeHtml(s.address)}</p>`);if(s.hours)links.push(`<p>Horarios: ${escapeHtml(s.hours)}</p>`);if(s.contact_text)links.push(`<p>${escapeHtml(s.contact_text)}</p>`);el.className='';el.innerHTML=links.join('')||'<p class="muted">La administradora todavía no ha configurado información pública de contacto.</p>';
